# Frontend environment variables (Vite)
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
#
# The admin login is handled entirely by the backend JWT system.
# There are NO secrets needed on the frontend.
# All sensitive config belongs in backend/.env
